spring:
  application:
    name: SentiStock-backend

  datasource:
    url: jdbc:mariadb://localhost:3306/sentistock
    username: root
    password: 3169
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
